---
title: "TEST & SECURE: Dependabot"
order: 8
stepSlug: "test-secure-dependabot"
duration: 3
summary: "依存関係の脆弱性チェックを Dependabot で有効化します。設定は数クリックで完了します。"
---

import Aside from '../../../components/handson/Aside.astro';

<Aside type="reference">

**📖 参考**: [プレイブック / Dependabot](https://theomonfort.github.io/theomonfort/playbook/dependabot?present=1) で Dependabot の仕組みと設定方法を確認できます。

</Aside>

### 8.1 — Dependabot を有効化する

1. リポジトリの **Settings** → **Security and quality** → **Advanced Security**
2. **Dependabot** セクション：
   - **Dependency graph** を有効化（Dependabot の前提条件）
   - **Dependabot security updates** を有効化
   - **Dependabot version updates** を有効化 → エディタが開き `.github/dependabot.yml` のテンプレートが表示されます
3. テンプレートの `package-ecosystem: ""` を **`"npm"`** に変更（本リポジトリは pnpm / npm エコシステム）。最終的に以下のような内容になります：

```yaml
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
```

4. **Commit changes…** → **「Create a new branch and start a pull request」** を選択 → PR を作成してマージ

<Aside type="negative">

**注意**: `package-ecosystem` が空のままだと **Commit changes** ボタンがグレーアウトします。`"npm"` などの有効な値を入れてから commit してください。また、セクション 7.2 で設定した Ruleset によって `main` への直接 commit はブロックされるので、PR 経由でマージします。

</Aside>

<Aside type="positive">

**ポイント**: 2 種類のアップデートは役割が違います：

- **Security updates** — GitHub Advisory Database に脆弱性が登録された瞬間に PR を自動生成（リアクティブ）
- **Version updates** — `.github/dependabot.yml` のスケジュール（例: weekly）に従って、脆弱性の有無に関係なく最新バージョンへの PR を生成（プロアクティブ）

両方有効化することで、脆弱性対応と最新化の両方をカバーできます。

</Aside>
